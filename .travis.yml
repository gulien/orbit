language: go

go:
  - 1.8
  - tip

matrix:
  allow_failures:
    - go: tip
  fast_finish: true

install:
  - go get github.com/Masterminds/glide
  - glide install
  - go install

script:
  - orbit run lint-install lint test

notifications:
  email: false