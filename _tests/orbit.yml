tasks:
  - use: "explorer"
    run:
      - dep status
  - use: "sputnik"
    private: true
    run:
      - dep status
  - use: "challenger"
    run:
      - faildep status
  - use: "zuma"
    shell: nope.sh
    run:
      - dep status
  - use: "falcon"
    shell: bash -c
    run:
      - dep status
  - use: "ariane"
    shell: ls
    run:
      - -l